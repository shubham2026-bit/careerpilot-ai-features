// Auth is handled client-side and via Supabase directly
// This file can remain empty or be used for custom endpoints

export const GET = async () => {
  return new Response('Auth endpoints are handled by Supabase', { status: 200 })
}

export const POST = async () => {
  return new Response('Auth endpoints are handled by Supabase', { status: 200 })
}
